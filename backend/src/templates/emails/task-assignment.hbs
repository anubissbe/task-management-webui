<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        /* Reset and base styles */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background-color: #f8fafc; }
        
        /* Container */
        .container { max-width: 600px; margin: 0 auto; background: white; }
        .content { padding: 40px 30px; }
        
        /* Header */
        .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 30px; text-align: center; }
        .header h1 { font-size: 24px; margin-bottom: 8px; }
        .header p { opacity: 0.9; font-size: 16px; }
        
        /* Main content */
        .main-content { margin: 30px 0; }
        .greeting { font-size: 18px; margin-bottom: 20px; color: #2d3748; }
        
        /* Task card */
        .task-card { background: #f7fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 25px; margin: 25px 0; }
        .task-title { font-size: 20px; font-weight: 600; color: #2d3748; margin-bottom: 15px; }
        .task-description { color: #4a5568; margin-bottom: 20px; line-height: 1.6; }
        
        /* Task meta */
        .task-meta { display: table; width: 100%; }
        .meta-row { display: table-row; }
        .meta-label { display: table-cell; font-weight: 600; color: #2d3748; padding: 8px 15px 8px 0; width: 120px; }
        .meta-value { display: table-cell; color: #4a5568; padding: 8px 0; }
        
        /* Action button */
        .action-section { text-align: center; margin: 35px 0; }
        .btn { display: inline-block; background: #667eea; color: white; text-decoration: none; padding: 14px 28px; border-radius: 6px; font-weight: 600; transition: background 0.3s; }
        .btn:hover { background: #5a67d8; }
        
        /* Footer */
        .footer { background: #2d3748; color: #a0aec0; padding: 30px; text-align: center; font-size: 14px; }
        .footer a { color: #81e6d9; text-decoration: none; }
        .footer .divider { margin: 15px 0; height: 1px; background: #4a5568; }
        
        /* Responsive */
        @media only screen and (max-width: 600px) {
            .content { padding: 20px; }
            .task-card { padding: 20px; }
            .btn { padding: 12px 24px; font-size: 16px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>📋 Task Assignment</h1>
            <p>You've been assigned a new task</p>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <div class="main-content">
                <div class="greeting">
                    Hi {{userName}},
                </div>
                
                <p>Great news! You've been assigned a new task by <strong>{{assignedBy}}</strong>. Here are the details:</p>
                
                <!-- Task Card -->
                <div class="task-card">
                    <div class="task-title">{{taskName}}</div>
                    
                    {{#if taskDescription}}
                    <div class="task-description">{{taskDescription}}</div>
                    {{/if}}
                    
                    <div class="task-meta">
                        {{#if projectName}}
                        <div class="meta-row">
                            <div class="meta-label">Project:</div>
                            <div class="meta-value">{{projectName}}</div>
                        </div>
                        {{/if}}
                        
                        <div class="meta-row">
                            <div class="meta-label">Assigned by:</div>
                            <div class="meta-value">{{assignedBy}}</div>
                        </div>
                        
                        {{#if dueDate}}
                        <div class="meta-row">
                            <div class="meta-label">Timeline:</div>
                            <div class="meta-value">{{dueDate}}</div>
                        </div>
                        {{/if}}
                    </div>
                </div>
                
                <!-- Action Button -->
                <div class="action-section">
                    <a href="{{taskUrl}}" class="btn">View Task Details</a>
                </div>
                
                <p>Click the button above to view the full task details and start working on it. If you have any questions, don't hesitate to reach out to your team!</p>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>This email was sent from <strong>ProjectHub-MCP</strong></p>
            <div class="divider"></div>
            <p>
                <a href="{{unsubscribeUrl}}">Unsubscribe from task assignment notifications</a>
            </p>
        </div>
    </div>
</body>
</html>