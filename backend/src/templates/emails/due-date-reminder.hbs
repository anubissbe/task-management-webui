<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        /* Reset and base styles */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background-color: #f8fafc; }
        
        /* Container */
        .container { max-width: 600px; margin: 0 auto; background: white; }
        .content { padding: 40px 30px; }
        
        /* Header - orange/warning theme */
        .header { background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%); color: white; padding: 30px; text-align: center; }
        .header h1 { font-size: 24px; margin-bottom: 8px; }
        .header p { opacity: 0.9; font-size: 16px; }
        
        /* Main content */
        .main-content { margin: 30px 0; }
        .greeting { font-size: 18px; margin-bottom: 20px; color: #2d3748; }
        
        /* Warning banner */
        .warning-banner { background: #fed7d7; border: 1px solid #feb2b2; border-radius: 8px; padding: 15px; margin: 20px 0; text-align: center; }
        .warning-banner .icon { font-size: 24px; margin-bottom: 5px; }
        .warning-banner .text { color: #c53030; font-weight: 600; }
        
        /* Task card */
        .task-card { background: #f7fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 25px; margin: 25px 0; }
        .task-title { font-size: 20px; font-weight: 600; color: #2d3748; margin-bottom: 15px; }
        .task-description { color: #4a5568; margin-bottom: 20px; line-height: 1.6; }
        
        /* Priority badge */
        .priority-badge { display: inline-block; padding: 4px 12px; border-radius: 20px; font-size: 12px; font-weight: 600; text-transform: uppercase; }
        .priority-high { background: #fed7d7; color: #c53030; }
        .priority-medium { background: #faf089; color: #975a16; }
        .priority-low { background: #c6f6d5; color: #276749; }
        
        /* Task meta */
        .task-meta { display: table; width: 100%; }
        .meta-row { display: table-row; }
        .meta-label { display: table-cell; font-weight: 600; color: #2d3748; padding: 8px 15px 8px 0; width: 120px; }
        .meta-value { display: table-cell; color: #4a5568; padding: 8px 0; }
        
        /* Action button */
        .action-section { text-align: center; margin: 35px 0; }
        .btn { display: inline-block; background: #ed8936; color: white; text-decoration: none; padding: 14px 28px; border-radius: 6px; font-weight: 600; transition: background 0.3s; }
        .btn:hover { background: #dd6b20; }
        
        /* Footer */
        .footer { background: #2d3748; color: #a0aec0; padding: 30px; text-align: center; font-size: 14px; }
        .footer a { color: #81e6d9; text-decoration: none; }
        .footer .divider { margin: 15px 0; height: 1px; background: #4a5568; }
        
        /* Responsive */
        @media only screen and (max-width: 600px) {
            .content { padding: 20px; }
            .task-card { padding: 20px; }
            .btn { padding: 12px 24px; font-size: 16px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>⏰ Task Reminder</h1>
            <p>Your task needs attention</p>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <div class="main-content">
                <div class="greeting">
                    Hi {{userName}},
                </div>
                
                <!-- Warning Banner -->
                <div class="warning-banner">
                    <div class="icon">⚠️</div>
                    <div class="text">Task Deadline Approaching</div>
                </div>
                
                <p>This is a friendly reminder about one of your assigned tasks that needs attention. Don't let it slip through the cracks!</p>
                
                <!-- Task Card -->
                <div class="task-card">
                    <div class="task-title">{{taskName}}</div>
                    
                    {{#if taskDescription}}
                    <div class="task-description">{{taskDescription}}</div>
                    {{/if}}
                    
                    <div class="task-meta">
                        {{#if projectName}}
                        <div class="meta-row">
                            <div class="meta-label">Project:</div>
                            <div class="meta-value">{{projectName}}</div>
                        </div>
                        {{/if}}
                        
                        {{#if priority}}
                        <div class="meta-row">
                            <div class="meta-label">Priority:</div>
                            <div class="meta-value">
                                <span class="priority-badge priority-{{priority}}">{{priority}}</span>
                            </div>
                        </div>
                        {{/if}}
                        
                        {{#if estimatedHours}}
                        <div class="meta-row">
                            <div class="meta-label">Estimated:</div>
                            <div class="meta-value">{{estimatedHours}} hours</div>
                        </div>
                        {{/if}}
                    </div>
                </div>
                
                <!-- Action Button -->
                <div class="action-section">
                    <a href="{{taskUrl}}" class="btn">Work on Task Now</a>
                </div>
                
                <p>⭐ <strong>Pro tip:</strong> Breaking down large tasks into smaller, manageable chunks can help you make steady progress and avoid last-minute rushes.</p>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>This email was sent from <strong>ProjectHub-MCP</strong></p>
            <div class="divider"></div>
            <p>
                <a href="{{unsubscribeUrl}}">Unsubscribe from due date reminders</a>
            </p>
        </div>
    </div>
</body>
</html>