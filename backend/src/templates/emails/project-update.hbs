<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        /* Reset and base styles */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background-color: #f8fafc; }
        
        /* Container */
        .container { max-width: 600px; margin: 0 auto; background: white; }
        .content { padding: 40px 30px; }
        
        /* Header - blue/info theme */
        .header { background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%); color: white; padding: 30px; text-align: center; }
        .header h1 { font-size: 24px; margin-bottom: 8px; }
        .header p { opacity: 0.9; font-size: 16px; }
        
        /* Main content */
        .main-content { margin: 30px 0; }
        .greeting { font-size: 18px; margin-bottom: 20px; color: #2d3748; }
        
        /* Update type badge */
        .update-type { display: inline-block; padding: 6px 16px; border-radius: 20px; font-size: 14px; font-weight: 600; text-transform: uppercase; margin-bottom: 20px; }
        .update-created { background: #c6f6d5; color: #276749; }
        .update-updated { background: #bee3f8; color: #2c5282; }
        .update-completed { background: #d69e2e; color: #744210; }
        .update-archived { background: #e2e8f0; color: #4a5568; }
        
        /* Update card */
        .update-card { background: #f7fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 25px; margin: 25px 0; }
        .project-title { font-size: 20px; font-weight: 600; color: #2d3748; margin-bottom: 15px; }
        .update-message { color: #4a5568; margin-bottom: 20px; line-height: 1.6; background: white; padding: 15px; border-radius: 6px; border-left: 4px solid #4299e1; }
        
        /* Project meta */
        .project-meta { display: table; width: 100%; }
        .meta-row { display: table-row; }
        .meta-label { display: table-cell; font-weight: 600; color: #2d3748; padding: 8px 15px 8px 0; width: 120px; }
        .meta-value { display: table-cell; color: #4a5568; padding: 8px 0; }
        
        /* Action button */
        .action-section { text-align: center; margin: 35px 0; }
        .btn { display: inline-block; background: #4299e1; color: white; text-decoration: none; padding: 14px 28px; border-radius: 6px; font-weight: 600; transition: background 0.3s; }
        .btn:hover { background: #3182ce; }
        
        /* Footer */
        .footer { background: #2d3748; color: #a0aec0; padding: 30px; text-align: center; font-size: 14px; }
        .footer a { color: #81e6d9; text-decoration: none; }
        .footer .divider { margin: 15px 0; height: 1px; background: #4a5568; }
        
        /* Responsive */
        @media only screen and (max-width: 600px) {
            .content { padding: 20px; }
            .update-card { padding: 20px; }
            .btn { padding: 12px 24px; font-size: 16px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🚀 Project Update</h1>
            <p>Stay informed about your projects</p>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <div class="main-content">
                <div class="greeting">
                    Hi {{userName}},
                </div>
                
                <p>There's been an update to one of your projects. Here's what happened:</p>
                
                <!-- Update Type Badge -->
                <div class="update-type update-{{updateType}}">{{updateType}} Update</div>
                
                <!-- Update Card -->
                <div class="update-card">
                    <div class="project-title">{{projectName}}</div>
                    
                    <div class="update-message">
                        "{{updateMessage}}"
                    </div>
                    
                    <div class="project-meta">
                        <div class="meta-row">
                            <div class="meta-label">Updated by:</div>
                            <div class="meta-value">{{updatedBy}}</div>
                        </div>
                        
                        <div class="meta-row">
                            <div class="meta-label">Update type:</div>
                            <div class="meta-value">{{updateType}}</div>
                        </div>
                    </div>
                </div>
                
                <!-- Action Button -->
                <div class="action-section">
                    <a href="{{projectUrl}}" class="btn">View Project</a>
                </div>
                
                <p>Stay up to date with all project changes to ensure smooth collaboration with your team. Click the button above to see the full project details and any additional updates.</p>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>This email was sent from <strong>ProjectHub-MCP</strong></p>
            <div class="divider"></div>
            <p>
                <a href="{{unsubscribeUrl}}">Unsubscribe from project update notifications</a>
            </p>
        </div>
    </div>
</body>
</html>